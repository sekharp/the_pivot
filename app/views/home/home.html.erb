<h1>Featured Projects</h1>

<div class="row">
  <% @top_projects.each do |project| %>
    <div class="card col s12">
      <div>
        <div class="col s4">
          <a href="<%= user_project_path(user: project.user.slug, id: project.slug) %>"><img class="max-width" src="<%= project.image.url(:original) %>"></a>
        </div>
        <div class="col s7 offset-s1">
          <h5><%= project.title %></h5>
          <p><%= project.description %></p>
        </div>
        <div class="card-action col s7 offset-s1">
          <div class="center">
              <h5>75% Funded</h5>
          </div>
          <div class="center">
          <span id='<%= project.title %>-button' class="col s5 left waves-effect waves-light btn custom-blue">
            <%= button_to "Learn More", user_project_path(user: project.user.slug, id: project.slug), method: :get %>
          </span>
          <span id='<%= project.title%>-lend-button' class="col s5 right waves-effect waves-light btn custom-blue">
            <%= button_to "Lend", "#" %>
          </span>
        </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
<div class="center">
  <h3>How It Works</h3>
  <p>
    GaneshaLoans is a micro-lending platform that allows you to lend to others in need and without traditional banking systems.
  </p>
</div>
<br>
<div class="center">
  <%= link_to "View More Projects", projects_path, class: "waves-effect waves-light btn custom-green" %>
</div>
<br>
<div class="center">
  <a class="waves-effect waves-light btn custom-blue" href="#primary-navigation">Back to top</a>
</div>
<br>
